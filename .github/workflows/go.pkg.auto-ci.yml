name: Go Package Release
on:
  push:
    branches:
      - main
      - 'features/**'
      - 'dependabot/**'
      - 'copilot/**'
  pull_request:
    branches:
      - 'features/**'
      - 'dependabot/**'
      - 'copilot/**'

permissions:
  contents: write

jobs:
  go-auto-ci:
    uses:  tommzn/github-ci/.github/workflows/go.pkg.auto-ci.yml@v1.0.100